# 简易 QQ 项目学习记录

这份文件不保存完整代码，而是记录每个可运行版本新增了什么功能、功能怎样实现，以及当前需要学习到什么程度。

## 记录规则

每完成并验证一个小版本，记录以下内容：

- 版本名称：与 Git commit message 保持一致；
- 新增功能：用户能够看到或使用什么；
- 实现思路：对象、状态、信号和数据怎样配合；
- 必须理解：需要能够用自己的话解释；
- 建议记忆：以后会反复使用的核心模式；
- 可以查资料：不必背诵的 API 和参数；
- 验证结果：实际运行检查了什么；
- Git 提交：记录对应的 commit 名称；已有历史编号时可以一并记录。

---

## 版本 1：Create initial Qt login window

### 新增功能

- 创建 Qt 登录窗口；
- 提供用户名和密码输入框；
- 点击 Login 后判断空输入、错误账号、错误密码和登录成功；
- 在窗口中显示判断结果；
- 使用变量累计密码错误次数。

### 实现思路

用户在 `QLineEdit` 中输入文字，点击 `QPushButton` 后发出 `clicked` 信号。`connect` 找到对应的 Lambda，Lambda 读取输入内容、执行登录判断，并修改 `QLabel` 显示的提示文字。

### 必须理解

- Qt 程序通过 `QApplication` 和事件循环持续等待用户操作；
- `connect` 建立信号与处理逻辑之间的关系，不会立即执行 Lambda；
- 界面数据流是“用户输入 → 控件保存 → 程序读取 → 判断 → 更新界面”；
- Lambda 捕获列表决定它能够访问哪些外部变量。

### 建议记忆

- `QWidget` 可以作为窗口和其他控件的父对象；
- `QLineEdit` 用于单行输入；
- `QPushButton` 提供点击信号；
- `QLabel` 用于显示文字。

### 可以查资料

- `setGeometry` 的四个参数；
- 各控件的完整 API；
- 字符串格式化的具体写法。

### 验证结果

- 已验证空输入、错误账号、错误密码和正确登录提示。

### Git 提交

- `534f843` — `Create initial Qt login window`

---

## 版本 2：Implement 60-second login lockout

### 新增功能

- 密码连续错误三次后禁用 Login 按钮；
- 显示 60 秒倒计时；
- 倒计时结束后恢复按钮并清零错误次数。

### 实现思路

`errorCount` 保存错误次数，`remainingSeconds` 保存剩余秒数。第三次错误时禁用按钮并启动 `QTimer`。定时器每隔一秒发出一次 `timeout` 信号，Lambda 将剩余时间减一并更新标签；到零后停止定时器并恢复按钮。

### 必须理解

- 变量可以保存程序在多个事件之间的状态；
- `QTimer` 依赖 Qt 事件循环，不使用阻塞式倒计时；
- `timeout` 信号只负责通知，真正修改状态的是 Lambda；
- 按引用捕获可以修改 Lambda 外面的原变量。

### 建议记忆

- 定时器基本模式：“创建 → 设置间隔 → 连接 timeout → start → stop”；
- `setEnabled(false)` 禁用控件，`setEnabled(true)` 恢复控件。

### 可以查资料

- `QTimer` 的其他计时模式；
- `QString::arg` 的格式化规则；
- 更精确的时间处理方法。

### 验证结果

- 已验证三次错误后按钮禁用、倒计时逐秒变化、到零后恢复按钮，以及错误次数重新从一开始。

### Git 提交

- `f5afc67` — `Implement 60-second login lockout`

---

## 版本 3：Open main window after login

### 新增功能

- 正确登录后显示主窗口；
- 隐藏登录窗口；
- 主窗口显示欢迎信息。

### 实现思路

程序启动时同时创建登录窗口对象和主窗口对象，但主窗口暂不显示。登录成功后调用主窗口的 `show()`，再调用登录窗口的 `hide()`。两个窗口对象在 `app.exec()` 运行期间始终存在。

### 必须理解

- 创建对象和显示对象是两件不同的事情；
- `hide()` 只改变可见状态，不会销毁窗口；
- 局部窗口对象在 `main()` 没有返回前仍然存在；
- Lambda 按引用捕获窗口后，可以操作外面的原窗口对象。

### 建议记忆

- 多窗口切换的基本思路：“显示目标窗口 → 隐藏当前窗口”；
- Qt 父子对象关系会影响子控件的显示位置和生命周期。

### 可以查资料

- `show`、`hide`、`close` 的更多差异；
- 更大型项目中的窗口类拆分方式。

### 验证结果

- 已验证错误登录时停留在登录窗口、正确登录后显示主窗口，以及关闭主窗口后程序退出。

### Git 提交

- `1cfac9e` — `Open main window after login`

---

## 版本 4：Add static friend list to main window

### 当前状态

- 已完成运行验证，待提交。

### 新增功能

- 主窗口显示 Friends 标题；
- 使用一个列表控件显示 Alice、Bob 和 Charlie；
- 用户可以选择不同好友。

### 实现思路

创建一个以主窗口为父对象的 `QListWidget`，再向同一个列表对象加入多个好友项。目前好友名字直接写在程序中；以后接入 SQLite 时，列表的显示职责不变，只把数据来源改成数据库查询结果。

### 必须理解

- 一个 `QListWidget` 可以管理和显示多个列表项；
- 控件负责显示，好友数据来源可以逐步替换；
- “数据从哪里来”和“数据怎样显示”是两个不同职责。

### 建议记忆

- `QListWidget` 适合当前阶段快速显示和选择简单列表；
- `addItem` 向现有列表加入一项。

### 可以查资料

- 列表项的样式、图标和附加数据；
- `QListWidget` 的选择和双击信号；
- 更复杂的 Model/View 架构。

### 验证结果

- 已验证主窗口显示 Alice、Bob 和 Charlie，并能选择不同好友项。

### Git 提交

- 提交名称：`Add static friend list to main window`

---

## 新版本记录模板

### 版本名称

与 Git commit message 保持一致。

### 新增功能

- 

### 实现思路



### 必须理解

- 

### 建议记忆

- 

### 可以查资料

- 

### 验证结果

- 

### Git 提交

- 提交名称：
